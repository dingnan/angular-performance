import { DollarAmountPipePipe } from './dollar-amount-pipe.pipe';

describe('DollarAmountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DollarAmountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
